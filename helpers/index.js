const RequestError = require('./RequestError');
const ctrlWrapper = require('./ctrlWrapper');
const imageSize = require('./imageSize');
const sendMail = require('./sendMail');
const sendMailFeedback = require('./sendMailFeedback');

module.exports = {
  RequestError,
  ctrlWrapper,
  imageSize,
  sendMail,
  sendMailFeedback,
}